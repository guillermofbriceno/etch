[package]
name = "etch-bridge"
version = "0.0.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
bridge-types = { path = "../../shared/bridge-types" }
interprocess = "2"
serde_json = "1.0"

[build-dependencies]
bindgen = "0.71"
